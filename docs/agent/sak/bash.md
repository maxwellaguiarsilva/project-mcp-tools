# Bash SAK

The Bash `sak` library is a single script containing utility functions for shell scripting.

## Overview

The `bash-swiss-army-knife` script provides a collection of reusable functions to simplify common tasks in Bash scripts, such as logging, error handling, string manipulation, and system checks.

It is designed to be sourced by other Bash scripts to provide a consistent set of utilities.
